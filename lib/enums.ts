export enum Gender {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum SortOrder {
  ASC = "ASC",
  DESC = "DESC",
}

export enum PaymentMethod {
  CASH = "CASH",
  VNPAY = "VNPAY",
}

export enum OrderStatus {
  PENDING = "PENDING",
  PAID = "PAID",
  CANCELED = "CANCELED",
  DELIVERED = "DELIVERED",
  DELIVERING = "DELIVERING",
}
